# Design: fix-parent-family-members-management

## 方案
在 `ParentFamilyPage` 中：
1. 为每个 `PARENT` 成员展示「移除」按钮；不为自己展示，避免与「退出家庭」混淆。
2. 为当前登录用户展示「退出家庭」按钮。
3. 点击操作前弹出确认对话框，避免误触。
4. 操作成功后调用 `refetch()` 刷新家庭数据。

## 边界
- 移除/退出失败时（如最后一个家长）展示后端返回的错误信息。
- 操作进行中禁用按钮，防止重复提交。
